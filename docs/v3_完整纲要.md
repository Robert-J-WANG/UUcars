# UUcars Marketplace V3 完整开发纲要

> **目标：在 V1/V2 已完成的全栈 Marketplace 基础上，进一步完善性能、基础设施、认证安全、前端体验、自动化测试、实时通知与生产工程能力。**
>
> 原则：**解决真实问题 · 每步有明确产出 · 代码与测试同步 · Git 流程规范 · 保持架构适度**

---

## 技术栈

**后端：**

- ASP.NET Core
- Entity Framework Core
- SQL Server
- ASP.NET Core Rate Limiting
- Hangfire + Hangfire.SqlServer
- Redis
- SignalR
- Application Insights
- Serilog
- JWT Authentication
- Refresh Token Rotation
- xUnit

**前端：**

- React + TypeScript
- Vite
- Tailwind CSS + shadcn/ui
- React Router
- Zustand
- TanStack Query
- Axios
- React Hook Form + Zod
- dnd-kit
- Vitest
- React Testing Library
- Playwright
- SignalR JavaScript Client
- Recharts

**文件和缓存服务：**

- Cloudflare R2
- Upstash Redis

**生产部署：**

- Azure SQL Database
- Azure Static Web Apps
- Azure Container Apps
- Docker
- GitHub Actions

---

## Git 分支策略

### 分支结构

```text
main       ← 正式里程碑版本，打 Tag
develop    ← 日常开发基准分支
feature/*  ← V3 功能分支
fix/*      ← 已合并功能的独立修复分支
docs/*     ← 文档整理分支
```

### 标准 Feature 工作流

```bash
# 1. 从最新 develop 切出功能分支
git checkout develop
git pull origin develop
git checkout -b feature/v3-xxx
git push -u origin feature/v3-xxx

# 2. 完成功能、测试和文档
git status
git add .
git commit -m "feat: ..."
git push origin feature/v3-xxx

# 3. 合并回 develop
git checkout develop
git pull origin develop
git merge --no-ff feature/v3-xxx \
  -m "merge: feature/v3-xxx into develop"
git push origin develop

# 4. 删除功能分支
git branch -d feature/v3-xxx
git push origin --delete feature/v3-xxx
```

### Commit Message 约定

```text
feat:     新功能
fix:      Bug 修复
perf:     性能优化
test:     测试
refactor: 重构但不改变功能
chore:    依赖、配置、脚本
docs:     文档
merge:    分支合并
release:  版本发布
```

### V3 里程碑计划

```text
v3.1  Step 63 完成
      性能、缓存、后台任务、认证、并发与安全

v3.2  Step 69 完成
      前端体验与自动化测试体系

v3.3  Step 75 完成
      实时通知、Admin 数据面板、OAuth、工程优化与 V3 收尾
```

---

# 第一阶段：性能、基础设施与安全（Step 57–63）

> **目标：优化数据库访问，建立缓存、限流和后台任务基础设施，并完善认证、并发控制与安全审计。**
>
> 每个 Step 使用独立 Feature 分支，完成验证后合并回 `develop`。

---

## Step 57 · 数据库查询诊断与索引优化

**产出：** 优化车辆、收藏和订单的主要只读查询，为数据量增长做好准备。

### 主要内容

1. 检查以下 Repository 的查询：
   - `EfCarRepository`
   - `EfFavoriteRepository`
   - `EfOrderRepository`
2. 查找不必要的 EF Core Tracking、实体完整加载、内存筛选和导航属性加载。
3. 在 `Cars` 表增加复合索引：
   ```text
   Status + CreatedAt
   Status + Brand
   Status + Price
   SellerId + Status + CreatedAt
   ```
4. 创建 Migration：
   ```text
   AddCarIndexes
   ```
5. 对只读列表查询使用 `AsNoTracking()`。
6. 对列表查询使用 `Select` 投影，只读取响应所需字段。
7. 检查 EF Core 生成的 SQL，确认筛选和分页在数据库端执行。

### 知识点

- 单列索引与复合索引
- 索引字段顺序
- EF Core Change Tracking
- DTO Projection
- 数据库端分页

### 测试与验证

- 检查 Migration 只包含目标索引。
- 运行现有 Repository、Service 和 API 测试。
- 检查查询生成的 SQL。
- 确认优化前后 API 返回结果一致。

### 验收标准

- 4 个复合索引配置正确。
- 只读列表查询使用 `AsNoTracking()`。
- 列表查询使用投影。
- 现有业务行为和测试不受影响。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-query-optimization
git push -u origin feature/v3-query-optimization

git add .
git commit -m "perf: add car indexes + AsNoTracking + select projection for list queries"
git push origin feature/v3-query-optimization
```

完成后按标准流程合并回 `develop`。

---

## Step 58 · Redis 缓存层

**产出：** 使用 Redis 缓存高频车辆列表，并在 Redis 不可用时自动回退数据库。

### 主要内容

1. 定义 `ICacheService`：
   ```csharp
   Task<T> GetOrSetAsync<T>(
       string key,
       Func<Task<T>> factory,
       TimeSpan expiration,
       CancellationToken cancellationToken = default);

   Task RemoveAsync(
       string key,
       CancellationToken cancellationToken = default);

   Task RemoveByPrefixAsync(
       string prefix,
       CancellationToken cancellationToken = default);
   ```
2. 实现 `RedisCacheService`：
   - JSON 序列化和反序列化
   - TTL
   - 完整 Key 删除
   - Prefix 批量删除
   - Redis 异常日志
   - Redis 故障时回退数据库
3. 新增 `CacheKeys`，管理 Published Cars 和 Pending Cars。
4. Published Cars 缓存 Key 包含：
   ```text
   Page
   PageSize
   Brand
   MinPrice
   MaxPrice
   MinYear
   MaxYear
   ```
5. Pending Cars 缓存 Key 包含：
   ```text
   Page
   PageSize
   ```
6. 在 `GetPublishedCarsAsync` 和 `GetPendingCarsAsync` 中使用 Cache-Aside。
7. 在以下业务数据变化后清理相关缓存：
   - 提交车辆审核
   - 审核通过
   - 审核拒绝
   - Admin 删除车辆
   - 创建订单并改变车辆状态
   - 取消订单并恢复车辆状态
8. 新增 `FakeCacheService`，供单元测试和集成测试使用。
9. Docker Compose 增加本地 Redis。
10. 生产环境使用 Upstash Redis，通过环境变量管理连接信息。

### 知识点

- Cache-Aside
- Cache Key 设计
- TTL
- 主动缓存失效
- Prefix Invalidation
- 缓存降级

### 测试与验证

- 缓存未命中时查询数据库并写入缓存。
- 缓存命中时直接返回缓存数据。
- 不同筛选条件使用不同 Key。
- 数据变化后相关缓存失效。
- Redis 不可用时业务回退数据库。
- 测试环境不依赖真实 Redis。

### 验收标准

- Published 和 Pending 列表支持缓存。
- 缓存 Key 与真实查询参数一致。
- Redis 故障不会中断主要业务。
- 缓存失效规则与业务状态变化一致。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-redis-cache
git push -u origin feature/v3-redis-cache

git add .
git commit -m "feat: Redis cache layer - ICacheService, RedisCacheService, car list caching"
git push origin feature/v3-redis-cache
```

完成后按标准流程合并回 `develop`。

---

## Step 59 · API 限流

**产出：** 对认证、邮件、浏览和写操作应用不同的限流策略，并统一处理 429。

### 主要内容

1. 定义 6 个策略：
   ```text
   Login
   Register
   ForgotPassword
   ResendVerification
   Browse
   Write
   ```
2. Login：固定窗口，按 IP 分区，每分钟 20 次。
3. Register：固定窗口，按 IP 分区，每小时 20 次。
4. Forgot Password：固定窗口，按 IP 分区，每小时 3 次。
5. Resend Verification：固定窗口，按 IP 分区，每小时 3 次。
6. Browse 使用 Token Bucket：
   ```text
   已登录用户：按 UserId 分区，每分钟 120 次
   未登录用户：按 IP 分区，每分钟 60 次
   ```
7. Write 使用 Token Bucket，按 UserId 分区，每分钟 30 次。
8. 被限流时统一返回：
   - `429 Too Many Requests`
   - `Retry-After`
   - 项目统一的 `ApiResponse`
9. Axios 响应拦截器读取 `Retry-After`，显示 toast 并返回标准 Error。
10. 中间件顺序：
    ```text
    CORS
    → Rate Limiter
    → Authentication
    → Authorization
    ```
11. 测试环境注册相同策略名称，保证带有限流特性的 Controller 正常启动。

### 知识点

- Fixed Window
- Token Bucket
- IP Partition
- User Partition
- `Retry-After`
- 中间件顺序

### 测试与验证

- 每个策略应用到正确接口。
- 超限后返回 429 和统一错误结构。
- 响应包含 `Retry-After`。
- 前端显示等待提示。
- 集成测试正常启动。

### 验收标准

- 6 个限流策略可用。
- 认证、邮件、浏览和写操作使用合适额度。
- 前后端能够统一处理 429。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-rate-limiting
git push -u origin feature/v3-rate-limiting

git add .
git commit -m "feat: rate limiting"
git push origin feature/v3-rate-limiting
```

完成后按标准流程合并回 `develop`。

---

## Step 60 · Hangfire 后台任务与 Application Insights

**产出：** 将邮件发送转为后台任务，定期清理过期 Token，并建立生产遥测能力。

### 主要内容

1. 安装：
   - `Hangfire`
   - `Hangfire.SqlServer`
   - `Microsoft.ApplicationInsights.AspNetCore`
   - `Serilog.Sinks.ApplicationInsights`
2. 配置 Hangfire：
   - 复用 SQL Server
   - Queue Poll Interval 15 秒
   - Sliding Invisibility Timeout 5 分钟
   - Worker Count 5
   - 使用 `default` Queue
3. Testing 环境跳过真实 Hangfire SQL Server 初始化。
4. 使用 `IBackgroundJobClient.Enqueue` 异步发送：
   - 注册验证邮件
   - 重发验证邮件
   - 密码重置邮件
5. 新增 `TokenCleanupService`，清理：
   - 过期密码重置 Token
   - 过期 Refresh Token
   - 已撤销 Refresh Token
6. 注册每日任务：
   ```text
   Job ID: cleanup-expired-tokens
   Cron: Cron.Daily
   Time Zone: UTC
   执行时间：UTC 00:00
   ```
7. 挂载 `/hangfire`。
8. Dashboard 授权：
   - Development 环境允许本地查看
   - 其他环境要求已认证 Admin
9. Application Insights 记录：
   - ASP.NET Core 请求
   - 异常
   - Dependency
   - 请求耗时
   - Serilog 结构化日志
10. Application Insights 未配置时，应用仍能正常启动。
11. Azure Container Apps 通过环境变量提供 Connection String。

### 知识点

- Fire-and-Forget Job
- Recurring Job
- Hangfire Worker
- Dashboard Authorization
- Application Insights
- Structured Logging
- UTC 定时任务

### 测试与验证

- 三种邮件操作创建后台任务。
- HTTP 请求不等待真实邮件发送。
- Token Cleanup 删除符合条件的数据。
- Testing 环境使用 Fake Job Client。
- Dashboard 权限正确。
- Application Insights 可以接收生产遥测。

### 验收标准

- 邮件发送不阻塞 HTTP 请求。
- Dashboard 可以查看任务状态。
- Token Cleanup 每天 UTC 00:00 执行。
- 测试环境不依赖 Hangfire 数据库。
- 生产日志可以进入 Application Insights。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-hangfire
git push -u origin feature/v3-hangfire

git add .
git commit -m "feat: Hangfire background jobs - async email sending + token cleanup + Application insights"
git push origin feature/v3-hangfire
```

完成后按标准流程合并回 `develop`。

---

## Step 61 · Refresh Token 机制

**产出：** 使用 Refresh Token Rotation 和 HttpOnly Cookie 自动续期用户会话。

### 主要内容

1. 新增 `RefreshTokens` 表：
   ```text
   Id
   Token
   UserId
   ExpiresAt
   IsRevoked
   CreatedAt
   ```
2. 创建 `AddRefreshTokensTable` Migration。
3. 登录成功后：
   - Access Token 放在响应 Body
   - Refresh Token 写入 HttpOnly Cookie
   - Refresh Token 保存到数据库
   - 默认有效期 7 天
4. 新增 `POST /auth/refresh`。
5. Refresh 时执行 Token Rotation：
   ```text
   读取 Cookie
   → 验证 Refresh Token
   → 撤销旧 Token
   → 创建新 Access Token
   → 创建新 Refresh Token
   → 更新 Cookie
   → 返回 Access Token
   ```
6. Logout 时撤销当前 Refresh Token、删除 Cookie 并清理前端状态。
7. Cookie 配置：

   **生产环境：**
   ```text
   HttpOnly = true
   Secure = true
   SameSite = None
   ```

   **开发环境：**
   ```text
   HttpOnly = true
   Secure = false
   SameSite = Strict
   ```
8. 删除 Cookie 时使用与创建 Cookie 匹配的属性。
9. CORS 指定前端 Origin 并启用 `AllowCredentials()`。
10. `authStore` 保存 User、Access Token 和初始化状态。
11. Access Token 只保存在内存中，不写入 `localStorage`。
12. 页面刷新后通过 `/auth/refresh` 恢复认证状态。
13. 使用模块级 `initializePromise` 避免重复初始化请求。
14. Axios 请求拦截器从 Zustand 读取最新 Access Token。
15. Axios 收到 401 后：
    - 使用 `_retry` 防止无限重试
    - 调用 Refresh
    - 更新 Access Token
    - 重试原请求
    - Refresh 失败后清理状态并跳转登录页
16. Axios 开启 `withCredentials: true`。
17. 考虑 Azure Container Apps 冷启动，将请求 timeout 设置为 30 秒。
18. Step 60 的定时任务同时清理无效 Refresh Token。

### 知识点

- Access Token 与 Refresh Token
- HttpOnly Cookie
- Refresh Token Rotation
- SameSite 与跨站 Cookie
- CORS Credentials
- 内存状态与持久化状态
- Axios Interceptor
- 认证状态初始化

### 测试与验证

- 运行后端编译和现有测试。
- 在浏览器中检查 Refresh Token Cookie 属性。
- 临时缩短 Access Token 有效期，验证自动 Refresh。
- 在数据库中检查旧 Token 被撤销、新 Token 生效。
- 验证 Logout 后 Cookie 被删除，旧 Token 不能继续使用。
- 验证页面刷新后可以恢复认证状态。

### 验收标准

- Access Token 可以自动续期。
- Refresh Token 不能被前端 JavaScript 读取。
- 每次 Refresh 都轮换 Token。
- Logout 后 Token 失效。
- Azure 跨站 Cookie 正常工作。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-refresh-token
git push -u origin feature/v3-refresh-token

git add .
git commit -m "feat: Refresh Token mechanism with token rotation and HttpOnly Cookie"
git push origin feature/v3-refresh-token
```

完成后按标准流程合并回 `develop`。

---

## Step 62 · 乐观锁与并发安全

**产出：** 使用 SQL Server RowVersion 防止车辆状态被并发请求静默覆盖。

### 主要内容

1. 为 `Car` 增加：
   ```csharp
   [Timestamp]
   public byte[] RowVersion { get; set; } = [];
   ```
2. 在 EF Core 中将其配置为 RowVersion/Concurrency Token。
3. 创建 `AddCarRowVersion` Migration，仅修改 `Cars` 表。
4. Admin 审核车辆时通过 `Car.RowVersion` 检测并发修改。
5. 创建订单时会同时把车辆从 Published 改为 Sold，通过 `Car.RowVersion` 防止两个 Buyer 同时购买同一辆车。
6. 捕获 `DbUpdateConcurrencyException` 并转换为项目统一的 `ConcurrencyException`。
7. 全局异常处理返回 `409 Conflict`。
8. Axios 统一处理 409：
   - 显示资源已被其他操作修改
   - 建议刷新后重试
   - 将 Error 返回调用方

### 知识点

- Optimistic Concurrency
- SQL Server RowVersion
- Concurrency Token
- Lost Update
- `DbUpdateConcurrencyException`
- HTTP 409

### 测试与验证

- `ApproveAsync_WhenConcurrencyConflict_ThrowsConcurrencyException`
- `RejectAsync_WhenConcurrencyConflict_ThrowsConcurrencyException`
- 手动验证正常审核和下单流程不受影响。
- 检查 API 的 409 统一响应。

### 验收标准

- `Cars` 表包含 RowVersion。
- Admin 审核冲突不会静默覆盖。
- 同一车辆的并发购买通过车辆 RowVersion 受到保护。
- 后端统一返回 409，前端显示明确提示。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-optimistic-lock
git push -u origin feature/v3-optimistic-lock

git add .
git commit -m "feat: optimistic locking with RowVersion for concurrent car operations"
git push origin feature/v3-optimistic-lock
```

完成后按标准流程合并回 `develop`。

---

## Step 63 · 安全加固与审计日志

**产出：** 增加 JWT 算法限制、安全响应头和 Admin 操作审计。

### 主要内容

1. JWT Validation 明确配置 `ValidAlgorithms`。
2. 继续验证 Issuer、Audience、Lifetime 和 Signing Key。
3. 增加安全响应头：
   ```text
   X-Content-Type-Options: nosniff
   X-Frame-Options: DENY
   Referrer-Policy: no-referrer
   ```
4. 新增 `AuditLogs` 表：
   ```text
   Id
   AdminId
   Action
   EntityType
   EntityId
   Detail
   CreatedAt
   ```
5. 定义 Admin 操作 Action：
   - CarApproved
   - CarRejected
   - CarDeleted
6. 新增 `AuditLogService`，统一写入审计记录。
7. 在 Approve、Reject 和 Delete 成功后写入日志。
8. 新增 `GET /admin/audit-logs`：
   - 按时间倒序
   - 支持分页
   - 仅允许 Admin 访问

### 知识点

- JWT Algorithm Restriction
- Security Headers
- Audit Trail
- 业务日志与技术日志的区别
- Admin 授权

### 测试与验证

- 运行现有后端测试，确认安全改动不破坏已有功能。
- 验证 API 返回安全响应头。
- 手动以 Admin 执行 Approve、Reject 和 Delete。
- 通过数据库或 `/admin/audit-logs` 检查审计记录。
- 验证非 Admin 无法访问审计接口。
- 验证分页和倒序结果。

### 验收标准

- JWT 只接受指定算法。
- 基础安全响应头存在。
- Admin 关键操作可以追踪。
- 审计接口受到 Admin 授权保护。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-security
git push -u origin feature/v3-security

git add .
git commit -m "feat: security hardening - security headers, JWT algorithm fix, audit logs"
git push origin feature/v3-security
```

完成后合并回 `develop`，发布 `v3.1`：

```bash
git checkout main
git merge --no-ff develop \
  -m "release: v3.1 - Infrastructure + Auth + Concurrency + Security"
git tag -a v3.1 \
  -m "v3.1: Hangfire + Refresh Token + Optimistic Lock + Security Hardening"
git push origin main
git push origin v3.1
git checkout develop
```

---

# 第二阶段：前端功能与体验优化（Step 64–67）

> **目标：完善车辆图片、表单可靠性、收藏交互和搜索体验。**

---

## Step 64 · 图片批量上传与拖拽排序

**产出：** 支持多图选择、预览、上传失败重试、创建前排序和已有图片排序。

### 主要内容

1. 后端批量上传接口：
   ```text
   POST /cars/{carId}/images/batch
   ```
2. 上传限制：
   - 每辆车最多 10 张
   - 每张最大 5 MB
   - JPEG、PNG、WebP
   - 每个文件单独验证
3. 上传前验证用户身份、车辆存在性和车辆归属。
4. 文件上传到 Cloudflare R2，并创建带 `DisplayOrder` 的 `CarImage`。
5. 前端文件输入支持 `multiple`。
6. 每张图片显示本地预览及等待、上传、成功或失败状态。
7. 单张失败不影响其他图片，并支持单独重试。
8. 创建车辆前：
   ```text
   选择图片
   → 本地预览
   → 本地排序
   → 创建车辆
   → 获得 carId
   → 按顺序上传图片
   ```
9. 编辑已有车辆时直接管理服务器图片。
10. 使用 `@dnd-kit/core` 和 `@dnd-kit/sortable`。
11. 支持拖动完整图片卡片。
12. 后端排序接口：
    ```text
    PUT /cars/{carId}/images/reorder
    ```
13. 验证图片属于目标车辆、ID 不重复、用户有权限且排序集合有效。
14. 排序后的第一张图片作为封面。

### 知识点

- Multipart Form Data
- 多文件上传
- Object URL
- 独立上传状态和 Retry
- Drag and Drop
- DisplayOrder
- 文件归属验证

### 测试与验证

- 批量上传合法图片。
- 超过 10 张、非法类型或超过大小限制时拒绝。
- 非 Owner 不能上传或排序。
- 非本车辆图片不能参与排序。
- 单张失败可以重试。
- 创建前顺序可以保留到上传阶段。

### 验收标准

- 图片上传前可预览和排序。
- 单张失败不影响其他图片。
- 已有图片可以重新排序。
- 第一张图片作为封面。
- 权限验证正确。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-image-upload
git push -u origin feature/v3-image-upload

git add .
git commit -m "feat: batch image upload with preview, progress, retry and drag-drop reorder"
git push origin feature/v3-image-upload
```

完成后按标准流程合并回 `develop`。

---

## Step 65 · 草稿自动保存与 ErrorBoundary

**产出：** 自动保存车辆表单内容，并在 React 渲染异常时提供可恢复界面。

### 主要内容

1. 草稿保存 Brand、Model、Year、Price、Mileage 和 Description 等可序列化字段。
2. 图片 File 和 Object URL 不写入 `localStorage`。
3. 表单变化后使用 2 秒 debounce 保存草稿。
4. 页面加载时检查并恢复草稿，显示提示。
5. 车辆创建成功后清除草稿，提交失败时保留草稿。
6. 提供主动清除草稿操作。
7. 使用 `react-error-boundary` 提供的 ErrorBoundary。
8. 创建项目统一的 `ErrorFallback` 和包装组件。
9. Fallback 提供错误说明和重新尝试按钮。
10. 只在 Development 环境显示具体错误内容。

### 知识点

- localStorage
- Debounce
- 表单恢复
- File 不可持久化问题
- `react-error-boundary`
- `FallbackComponent`
- `resetErrorBoundary`

### 测试与验证

- 表单变化后保存草稿。
- 刷新后恢复草稿。
- 创建成功后删除草稿。
- 创建失败后保留草稿。
- 图片不写入 localStorage。
- 子组件抛出异常后显示 fallback。

### 验收标准

- 表单内容不会因刷新意外丢失。
- 草稿恢复有明确提示。
- 图片文件不会被错误持久化。
- React 渲染错误不会产生无提示白屏。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-draft-autosave-errorboundary
git push -u origin feature/v3-draft-autosave-errorboundary

git add .
git commit -m "feat: draft autosave and ErrorBoundary"
git push origin feature/v3-draft-autosave-errorboundary
```

完成后按标准流程合并回 `develop`。

---

## Step 66 · 收藏功能乐观更新

**产出：** 收藏按钮立即响应，在请求失败时恢复原状态，并在请求结束后与服务器重新同步。

### 主要内容

1. 查询当前车辆收藏状态：
   ```text
   GET /favorites/{carId}
   ```
2. 未登录用户不发送收藏状态请求。
3. 使用本地 `isFavorited` 控制按钮，服务器结果返回后同步本地状态。
4. 添加收藏：
   ```text
   POST /favorites/{carId}
   ```
5. 取消收藏：
   ```text
   DELETE /favorites/{carId}
   ```
6. `onMutate`：
   - 取消相关查询
   - 保存收藏列表缓存快照
   - 保存旧按钮状态
   - 立即切换 UI
7. `onError`：
   - 显示错误提示
   - 恢复按钮状态
   - 恢复缓存快照
8. `onSettled` 调用统一失效函数，使 Favorites 列表和单辆车收藏状态重新同步。
9. Mutation 进行中限制重复点击。
10. Query Key：
    ```text
    ["favorite", carId]
    ["favorites", ...]
    ```

### 知识点

- Optimistic Update
- Mutation Lifecycle
- Cache Snapshot
- Rollback
- Query Invalidation
- 本地 UI State 与服务器 State

### 测试与验证

- 添加和取消收藏时按钮立即变化。
- 请求失败时恢复原状态。
- 请求结束后相关查询被正确失效。
- 未登录用户不查询收藏状态。
- 重复点击不会产生冲突请求。

### 验收标准

- 收藏交互无需等待服务器响应。
- 添加和取消均支持失败回滚。
- 最终状态可以与服务器重新同步。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-optimistic-favorite
git push -u origin feature/v3-optimistic-favorite

git add .
git commit -m "feat: optimistic bidirectional favorite toggle with correct initial state"
git push origin feature/v3-optimistic-favorite
```

完成后按标准流程合并回 `develop`。

---

## Step 67 · 搜索体验优化

**产出：** 增加 Year 范围筛选、品牌搜索词高亮和完整 URL 状态同步。

### 主要内容

1. 增加 Min Year 和 Max Year。
2. 后端 `CarQueryRequest` 支持 `MinYear` 和 `MaxYear`。
3. 首页使用 `brand` 作为品牌搜索和筛选参数。
4. URL 同步：
   ```text
   brand
   minPrice
   maxPrice
   minYear
   maxYear
   page
   ```
5. PageSize 使用首页固定常量，不写入 URL。
6. 页面刷新时从 URL 恢复筛选。
7. Brand、Price 和 Year 输入使用 debounce。
8. 后端对 Brand 执行不区分大小写的模糊匹配。
9. CarCard 在 `Brand` 和 `Title` 中高亮当前品牌搜索词。
10. 高亮逻辑转义正则特殊字符，不注入未经处理的 HTML。
11. 筛选变化后返回第一页，翻页时保留其他参数。
12. 清除筛选时同步清理 URL。

### 知识点

- Query String
- `useSearchParams`
- Debounce
- 正则表达式转义
- 搜索结果高亮
- 可分享 URL

### 测试与验证

- Year 范围正确传给后端。
- MinYear 和 MaxYear 可以独立使用。
- 刷新和分享 URL 后筛选条件保留。
- Brand 和 Title 中的匹配文字正确高亮。
- 特殊字符不会破坏高亮逻辑。
- 修改筛选后回到第一页。

### 验收标准

- Year 范围筛选可用。
- 品牌搜索词正确高亮。
- 筛选状态与 URL 一致。
- 输入不会产生过多请求。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-search-ux
git push -u origin feature/v3-search-ux

git add .
git commit -m "feat: year filter UI, search keyword highlight, and URL sync"
git push origin feature/v3-search-ux
```

完成后按标准流程合并回 `develop`。

---

# 第三阶段：测试体系（Step 68–69）

> **目标：建立前端单元测试和完整业务流程的端到端测试。**

---

## Step 68 · Vitest 与 React Testing Library

**产出：** 建立前端单元测试环境，并覆盖关键组件和认证路由。

### 主要内容

1. 配置 Vitest：
   - jsdom
   - Setup File
   - Path Alias
   - Coverage
2. 安装 React Testing Library、jest-dom、user-event 和 Coverage Provider。
3. 在 Vitest 配置中排除 `e2e/**`，Playwright 测试只由 Playwright 执行。
4. npm scripts：
   ```json
   {
     "test": "vitest",
     "test:coverage": "vitest --coverage"
   }
   ```
5. 一次性运行：
   ```bash
   npm test -- --run
   ```
6. CarCard：4 个测试
   - 基础信息
   - 无图片 fallback
   - 正确显示图片
   - 品牌搜索词高亮
7. LoginPage：4 个测试
   - 空表单验证
   - 无效邮箱验证
   - 正确提交登录
   - API 错误提示
8. ProtectedRoute：2 个测试
   - 未认证用户跳转登录
   - 已认证用户看到受保护内容
9. 保留 1 个测试环境 Sanity Test。
10. 测试关注用户行为，不依赖组件内部实现。
11. 测试文件不保留临时 `screen.debug`。

### 知识点

- Unit Test 与 Component Test
- jsdom
- Accessible Query
- `userEvent`
- Mock
- Coverage
- Vitest 与 Playwright 的职责边界

### 测试与验证

- `npm test -- --run`
- `npm run test:coverage`
- 确认 4 个测试文件、11 个测试通过。
- 确认 Vitest 不再扫描 Playwright 文件。
- 确认测试输出没有临时 DOM 调试内容。

### 验收标准

- Vitest 和 RTL 配置完成。
- 11 个基础测试通过。
- Coverage 报告可以生成。
- 单元测试不依赖真实 API。
- Vitest 与 Playwright 测试范围互不冲突。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-frontend-tests
git push -u origin feature/v3-frontend-tests

git add .
git commit -m "test: setup vitest + RTL, add tests for CarCard, LoginPage, ProtectedRoute"
git push origin feature/v3-frontend-tests
```

完成后按标准流程合并回 `develop`。

---

## Step 69 · Playwright 端到端测试

**产出：** 使用真实前后端和数据库验证五条核心用户流程。

### 主要内容

1. 配置 Playwright：
   - Chromium
   - Base URL
   - Screenshot on failure
   - Trace
   - Retry 关闭
   - 单 Worker
2. E2E 使用本地 React、ASP.NET Core API、SQL Server 和 Redis。
3. 创建 `LoginPage` Page Object。
4. 使用 Playwright Request API 创建测试前置数据。
5. Development 环境提供测试所需的邮箱验证 Token 获取接口，生产环境不暴露。
6. 使用时间戳生成唯一邮箱和测试数据。
7. 五条核心用户旅程：
   1. 用户注册并看到验证提示
   2. Seller 登录、发布车辆并提交审核
   3. Admin 审核车辆通过
   4. Buyer 浏览车辆并下单
   5. Seller 查看收到的销售订单
8. 失败时保留 Screenshot、Trace、Playwright Report 和 Test Results。
9. npm scripts：
   ```json
   {
     "e2e": "playwright test",
     "e2e:ui": "playwright test --ui",
     "e2e:headed": "playwright test --headed"
   }
   ```
10. GitHub Actions 建立 E2E Job 骨架：
    - 安装 Chromium
    - 配置测试产物上传
    - 预留 Staging URL 和凭据
11. Staging 环境建立前不执行依赖完整环境的 E2E 命令。

### 知识点

- End-to-End Test
- Page Object Model
- API Setup
- 测试数据隔离
- 浏览器自动化
- CI 与 Staging Environment

### 测试与验证

- 五条流程可以连续通过。
- 重复运行不会因数据冲突失败。
- 测试接口只在 Development 可用。
- 失败时生成截图和报告。
- Vitest 不加载 E2E 文件。

### 验收标准

- 本地 Playwright 流程稳定。
- 核心业务由真实前后端共同验证。
- 测试数据能够隔离。
- CI 为未来 Staging E2E 做好准备。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-e2e-tests
git push -u origin feature/v3-e2e-tests

git add .
git commit -m "test: Playwright E2E tests for 5 core user journeys"
git push origin feature/v3-e2e-tests
```

完成后合并回 `develop`，发布 `v3.2`：

```bash
git checkout main
git merge --no-ff develop \
  -m "release: v3.2 - Frontend features + Testing"
git tag -a v3.2 \
  -m "v3.2: Frontend features and automated testing"
git push origin main
git push origin v3.2
git checkout develop
```

---

# 第四阶段：实时功能与后台管理（Step 70–71）

> **目标：实现实时通知，并通过 Admin 数据面板展示平台核心业务数据。**

---

## Step 70 · SignalR 实时通知与通知持久化

**产出：** 订单和车辆审核事件可以持久化为通知，并实时推送给目标用户。

### 主要内容

1. 新增 `Notification`：
   ```text
   Id
   UserId
   Type
   Message
   RelatedId
   IsRead
   CreatedAt
   ```
2. `Message` 最大长度 300。
3. 定义通知类型：
   ```text
   NewOrder
   OrderCancelled
   CarApproved
   CarRejected
   ```
4. 创建 EF Core Configuration、DbSet 和 `AddNotificationsTable` Migration。
5. 为 `UserId + IsRead + CreatedAt` 配置复合索引。
6. 新增 SignalR Hub：
   ```text
   /hubs/notification
   ```
7. Hub 要求 JWT 认证。
8. 前端通过 `accessTokenFactory` 提供 Access Token。
9. 后端只在 Hub 路径读取 Query String 中的 `access_token`。
10. 统一 Group 名称：
    ```csharp
    public static string ForUser(int userId)
        => $"user-{userId}";
    ```
11. `OnConnectedAsync`：
    - 从 JWT 身份读取 UserIdentifier
    - 转换成整数 UserId
    - 非法身份终止连接
    - 将 Connection 加入用户 Group
12. 同一用户的多个 Connection 加入同一个 Group。
13. `INotificationService`：
    ```text
    写数据库
    → 保存
    → 推送 ReceiveNotification
    ```
14. SignalR 推送失败只记录 Warning，不影响已经成功的业务操作。
15. 业务事件：
    - 创建订单 → Seller
    - 取消订单 → Seller
    - 审核通过 → Seller
    - 审核拒绝 → Seller
16. REST API：
    ```text
    GET /notifications
    PUT /notifications/{id}/read
    PUT /notifications/read-all
    ```
17. `GET /notifications` 返回当前用户全部未读通知。
18. 用户只能操作自己的通知。
19. `read-all` 使用 `ExecuteUpdateAsync`。
20. 前端 Query Key 包含 UserId。
21. 通知逻辑保留在 `NotificationBell`：
    - 查询未读通知
    - 显示角标和列表
    - 单条或全部标记已读
    - 建立 SignalR 连接
    - 监听 `ReceiveNotification`
    - 收到事件后使通知查询失效
    - 卸载时停止连接
22. SignalR 使用自动重连，每次连接或重连从 Zustand 读取最新 Token。

### 知识点

- SignalR Hub
- WebSocket
- JWT Query String
- ConnectionId
- User Group
- 多标签页连接
- 自动重连
- 通知持久化
- 实时事件与 REST 查询结合

### 测试与验证

AdminCarService：

- Approve 发送 `CarApproved`。
- Reject 发送 `CarRejected`。

OrderService：

- 创建订单发送 `NewOrder`。
- 取消订单发送 `OrderCancelled`。

使用 `FakeNotificationService` 验证：

- 接收用户
- 通知类型
- `RelatedId`
- 业务失败时不发送成功通知

手动验证：

- Seller 实时收到通知。
- 多标签页同时收到通知。
- 断线重连后重新加入 Group。
- 离线后重新登录仍可读取未读通知。
- 单条已读和全部已读正确。

### 验收标准

- 四类通知可以持久化。
- 在线用户可以实时接收。
- 离线用户可以读取未读通知。
- 多标签页连接正常。
- 用户只能操作自己的通知。
- 后端通知测试通过。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-signalr-notifications
git push -u origin feature/v3-signalr-notifications

git add .
git commit -m "feat: SignalR real-time notifications with persistence"
git push origin feature/v3-signalr-notifications
```

完成后按标准流程合并回 `develop`。

---

## Step 71 · Admin 数据面板

**产出：** Admin 可以查看平台核心指标、趋势图表和审计日志。

### 主要内容

1. 在受 Controller 级 Admin 角色授权保护的 `AdminController` 中新增 `GET /admin/stats`。
2. 使用 `AdminStatsResponse` 一次返回：
   ```text
   totalCars
   pendingCars
   publishedCars
   totalUsers
   monthlyOrders
   monthlyRevenue
   dailyNewCars
   dailyRevenue
   brandDistribution
   ```
3. 6 个核心指标使用明确的业务口径：
   - `totalCars`：除 `Deleted` 外的全部车辆。
   - `pendingCars`：`PendingReview` 车辆。
   - `publishedCars`：仅 `Published` 车辆，不包含 `Sold`。
   - `totalUsers`：全部用户，包括 Admin Seed。
   - `monthlyOrders`：当前 UTC 月内按 `CreatedAt` 创建的全部订单。
   - `monthlyRevenue`：当前 UTC 月内按 `UpdatedAt` 完成的订单，只统计 `Completed`，金额使用订单快照 `Order.Price`。
4. 月度查询使用 UTC 半开区间：
   ```text
   timestamp >= startOfMonth
   timestamp < startOfNextMonth
   ```
5. `dailyNewCars` 和 `dailyRevenue` 返回包含今天在内的最近 30 个 UTC 日期：
   - `dailyNewCars` 按 `Car.CreatedAt` 分组，保留后来变成 `Deleted` 的历史创建事件。
   - `dailyRevenue` 按 Completed 订单的 `UpdatedAt` 分组并汇总 `Order.Price`。
   - Repository 只返回数据库实际存在的日期，Service 补齐缺失日期并填入 `0`。
   - 日期桶使用 `DateOnly`，避免前端时区转换造成日期偏移。
6. 品牌分布只统计 `Published + Sold`，先按数量降序、品牌名升序稳定排序，再取前 10。
7. 使用 `TimeProvider` 获取当前 UTC 时间，使 30 天时间窗口可以在单元测试中固定。
8. 使用 `Controller → Service → Repository` 分层：
   - `EfAdminStatsRepository` 执行 `Count`、`Sum` 和 `GroupBy`。
   - `AdminStatsService` 计算时间边界、补齐日期并组合 Dashboard DTO。
   - 聚合查询直接在数据库执行，不先加载 Entity，也不需要额外使用 `AsNoTracking()`。
9. 使用 Cache-Aside 缓存完整的 `AdminStatsResponse`：
   - 缓存常量为 `CacheKeys.AdminStats`。
   - TTL 为 5 分钟。
   - 所有 Admin 共用同一个统计结果。
   - 本步骤接受最多 5 分钟延迟，不为车辆、订单和用户写操作增加主动失效。
10. 新增 `/admin/dashboard` 页面，并将 `/admin` 默认重定向到 Dashboard。
11. Stats 和 Audit Logs 使用两个独立的 TanStack Query：
   - Stats 使用固定 Query Key。
   - Audit Logs 的 Query Key 包含 `page` 和 `pageSize`。
   - 日志翻页不会重新请求 Stats。
12. Admin Dashboard 渲染：
   - 6 个统计卡片
   - 每日新增车辆折线图
   - 每日成交额折线图
   - 品牌分布饼图
   - 审计日志表格和分页
13. 使用 Recharts 的 `ResponsiveContainer`、`LineChart` 和 `PieChart`，并为图表父容器设置明确高度。
14. Stats 与 Audit Logs 分别处理 Loading、Error 和 Success 状态，审计日志额外处理 Empty 状态。
15. 使用新西兰格式显示数据：
    - 数字：`en-NZ`
    - 金额：`en-NZ` + `NZD`
    - UTC 日期桶：直接按 `yyyy-MM-dd` 字符串格式化，不创建本地 `Date`
    - 审计日志时间：转换为用户本地时间后使用 `en-NZ`
16. 审计日志继续复用 Step 63 已实现的 `GET /admin/audit-logs`，不为 Dashboard 重复实现日志接口。

### 知识点

- SQL Aggregate
- `Count`、`Sum` 和 `GroupBy`
- UTC 半开时间区间
- `DateOnly` 日期桶
- 时间序列补零
- Dashboard DTO
- `TimeProvider`
- Cache-Aside 与短期 TTL
- TanStack Query Key
- Recharts
- 新西兰数字、金额和日期格式

### 测试与验证

- Service 单元测试使用固定 UTC 时间，验证最近 30 天的起止日期、连续 30 项和缺失日期补零。
- API 集成测试使用真实 SQL Server，验证：
  - `Deleted` 不进入 `totalCars`。
  - 月度订单和月度成交额使用正确边界与统计口径。
  - 每日新增车辆包含历史创建后被删除的车辆。
  - 每日成交额只汇总 Completed 订单。
  - 品牌分布只返回稳定排序后的前 10。
- API 集成测试验证普通用户访问 `GET /admin/stats` 返回 403。
- 通过真实 Redis 日志手动验证首次请求 Cache MISS 并执行 SQL，第二次请求 Cache HIT 且不再执行统计 SQL。
- 手动验证 6 张统计卡片、两个折线图、一个饼图及审计日志分页正确渲染。
- 运行后端测试、前端 lint 和 build；本步骤不新增 Dashboard 前端自动化测试。

### 验收标准

- Admin 可以查看 6 个核心指标。
- 月度统计使用正确的 UTC 边界和业务口径。
- 两组趋势数据连续返回最近 30 个 UTC 日期。
- 品牌分布只包含 Published 和 Sold，并返回前 10。
- 两个折线图和一个饼图正常显示。
- 审计日志可以分页查看。
- 非 Admin 无法访问统计接口或 Dashboard。
- 完整统计结果缓存 5 分钟，缓存命中时不再执行统计查询。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-admin-dashboard
git push -u origin feature/v3-admin-dashboard

git add .
git commit -m "feat: admin statistics dashboard with charts and audit logs"
git push origin feature/v3-admin-dashboard
```

完成后按标准流程合并回 `develop`。

---

# 第五阶段：认证扩展、交付优化与总结（Step 72–75）

> **目标：增加 Google 登录，优化 CI/CD 和前端性能，并完成 V3 文档与版本收尾。**

---

## Step 72 · Google OAuth 登录

**产出：** 用户可以使用 Google 账号登录，并复用现有 Access Token 与 Refresh Token 体系。

### 主要内容

1. 在 Google Cloud Console 创建 OAuth Client。
2. 配置本地和 Azure Static Web Apps 生产 Origin。
3. 后端新增 `POST /auth/google`。
4. 使用 Google 官方验证库验证 ID Token 和 Audience。
5. 读取 Email、Name、Google Subject 和 Email Verified。
6. 使用独立认证服务封装 Google Token 验证，便于测试。
7. 查找或创建用户。
8. 同邮箱已经存在时，只在 Google Email 已验证后复用现有用户。
9. Google 登录成功后签发 Access Token 和 Refresh Token。
10. Google 创建的账号不允许使用不存在的本地密码登录。
11. 前端安装 `@react-oauth/google`。
12. 登录页和注册页增加 Google 登录按钮。
13. Client ID 使用 `VITE_GOOGLE_CLIENT_ID`。
14. 本地使用 `.env.local`，生产值配置在 Azure Static Web Apps 环境变量中。

### 知识点

- OAuth 2.0
- OpenID Connect
- ID Token
- Audience
- External Login
- Account Linking
- 第三方身份与本地 JWT

### 测试与验证

- 有效 Token 可以登录，错误 Audience 被拒绝。
- 未验证邮箱不能自动关联。
- 同邮箱不会重复创建账号。
- Google 用户获得正常 Access/Refresh Token。
- 普通登录流程不受影响。

### 验收标准

- Google 登录可以完成。
- 复用现有 Token 体系。
- 同邮箱账号处理安全。
- 真实配置不进入 Git。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-google-oauth
git push -u origin feature/v3-google-oauth

git add .
git commit -m "feat: Google OAuth login with existing JWT and refresh token flow"
git push origin feature/v3-google-oauth
```

完成后按标准流程合并回 `develop`。

---

## Step 73 · CI/CD 优化与 Nginx 原理

**产出：** 优化 GitHub Actions 构建效率，并通过本地 Docker 演示理解 Nginx。

### 主要内容

1. GitHub Actions 缓存：
   - NuGet Package Cache
   - npm Download Cache
   - 根据 `.csproj` 和 Lock File 生成 Cache Key
2. 前端 Build 和后端 Test 并行执行。
3. 记录优化前后的构建时间。
4. 检查缓存失效条件。
5. Docker Compose 增加本地 Nginx 演示容器。
6. Nginx 本地配置：
   - SPA 路由回退
   - API Reverse Proxy
   - 静态资源缓存
   - Gzip
7. 理解 Reverse Proxy、Upstream、`try_files`、Cache-Control 和 Gzip。
8. 区分本地学习架构与真实生产架构。
9. UUcars 生产环境继续使用：
   - Azure Static Web Apps
   - Azure Container Apps
   - Azure SQL Database
   - Docker
   - GitHub Actions
10. 不将 Nginx 描述为 UUcars 生产部署组件。

### 知识点

- CI Cache
- Cache Key
- 并行 Job
- Reverse Proxy
- SPA Fallback
- Azure 托管服务与本地代理的职责差异

### 测试与验证

- GitHub Actions 缓存命中。
- Lock File 变化后缓存更新。
- Build 和 Test 可以并行。
- 本地 Nginx 可以访问前端并代理 API。
- React Router 刷新不会返回 404。
- Azure 生产部署流程不受影响。

### 验收标准

- CI 构建时间得到优化并有前后数据。
- 本地 Nginx 演示可运行。
- 项目文档准确描述真实生产架构。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-cicd-optimisation
git push -u origin feature/v3-cicd-optimisation

git add .
git commit -m "chore: optimise CI caching and add local Nginx demonstration"
git push origin feature/v3-cicd-optimisation
```

完成后按标准流程合并回 `develop`。

---

## Step 74 · Bundle 优化与 Web Vitals

**产出：** 分析前端 Bundle，实施路由懒加载和图片加载优化。

### 主要内容

1. 安装 `rollup-plugin-visualizer`，生成 Bundle 报告。
2. 检查最大依赖、重复依赖、Tree Shaking 和不必要的全量导入。
3. 页面组件使用 `React.lazy()` 和 `Suspense`。
4. 增加通用 Page Skeleton。
5. 验证页面 Chunk 按需加载。
6. 检查 Recharts、dnd-kit、SignalR 和其他大型依赖的导入。
7. 图片增加 `loading="lazy"`。
8. 车辆封面设置稳定宽高比，减少 CLS。
9. 学习并测量 LCP、CLS 和 INP。
10. 使用 Chrome DevTools Lighthouse 手动检查。
11. 不增加 Lighthouse CI。

### 知识点

- Bundle Analysis
- Code Splitting
- Route-level Lazy Loading
- Tree Shaking
- Dynamic Import
- LCP、CLS、INP

### 测试与验证

- Production Build 成功。
- Bundle 报告可以生成。
- 页面按需加载且有 fallback。
- 图片不会造成明显布局跳动。
- RTL 和 E2E 测试继续通过。

### 验收标准

- Bundle 结构可分析。
- 主要页面使用路由懒加载。
- 图片加载策略得到优化。
- 能解释主要 Web Vitals 指标。

**Git 操作：**

```bash
git checkout develop
git checkout -b feature/v3-frontend-performance
git push -u origin feature/v3-frontend-performance

git add .
git commit -m "perf: add bundle analysis, route lazy loading and image optimisations"
git push origin feature/v3-frontend-performance
```

完成后按标准流程合并回 `develop`。

---

## Step 75 · V3 总结与项目收尾

**产出：** 文档、数据库说明、测试说明和 README 与最终系统保持一致，并发布 V3 最终里程碑。

### 主要内容

1. 以实际代码、EF Core Migration、配置、测试和 Git 历史作为事实来源。
2. 修改文档前检查并记录差异。
3. 按以下顺序更新：
   ```text
   docs/steps_v1.md
   docs/steps_v2.md
   docs/steps_v3.md
   docs/database-schema.md
   README.md
   ```
4. 保留历史学习笔记，但明确区分学习过程、最终实现、未采用方案和当前限制。
5. README 包含：
   - 项目定位
   - V1/V2/V3 技术演进
   - 系统架构
   - 功能清单
   - 本地启动
   - 环境变量
   - Migration
   - 测试
   - 生产部署
   - 已知限制
6. 明确 UUcars 是个人全栈 Portfolio Project，不描述为实习或就业经历。
7. 生产部署只描述：
   - Azure SQL Database
   - Azure Static Web Apps
   - Azure Container Apps
   - Docker
   - GitHub Actions
8. 不声称使用 VPS 或 Nginx 进行生产部署。
9. 完善 Scalar/OpenAPI 的接口摘要、说明、示例和错误码。
10. 记录已知限制：
    - Refresh Token 设备管理
    - LIKE 搜索
    - SignalR 单实例 Group
    - 没有 Redis Backplane
    - E2E CI 对 Staging 的依赖
    - 外部服务依赖
11. 运行：
    ```bash
    dotnet build
    dotnet test

    cd uucars-web
    npm run lint
    npm test -- --run
    npm run build
    npm run e2e
    ```
12. 手动检查注册、登录、车辆发布、图片上传、Admin 审核、收藏、订单、通知、Google 登录和 Admin Dashboard。

### 知识点

- Documentation as Code
- Source of Truth
- Release Checklist
- Regression Testing
- Known Limitations
- Semantic Versioning

### 验收标准

- 开发步骤与真实实现一致。
- 数据库文档与 Migration 一致。
- README 与最终生产架构一致。
- 历史学习内容和最终系统清楚区分。
- 相关测试和构建检查通过。
- 不包含不真实的项目经历或部署声明。

**Git 操作：**

```bash
git checkout develop
git checkout -b docs/v3-project-summary
git push -u origin docs/v3-project-summary

git add .
git commit -m "docs: complete V3 documentation and project summary"
git push origin docs/v3-project-summary
```

完成后合并回 `develop`，发布 `v3.3`：

```bash
git checkout main
git pull origin main
git merge --no-ff develop \
  -m "release: v3.3 - V3 complete"
git tag -a v3.3 \
  -m "V3.3: Real-time notifications, admin analytics, OAuth and engineering optimisation"
git push origin main
git push origin v3.3
git checkout develop
```

---

# 完整步骤总览

```text
第一阶段：性能、基础设施与安全             Step 57–63
  Step 57  数据库查询诊断与索引优化
  Step 58  Redis 缓存层
  Step 59  API 限流
  Step 60  Hangfire 后台任务与 Application Insights
  Step 61  Refresh Token 机制
  Step 62  乐观锁与并发安全
  Step 63  安全加固与审计日志
  里程碑：v3.1

第二阶段：前端功能与体验优化               Step 64–67
  Step 64  图片批量上传与拖拽排序
  Step 65  草稿自动保存与 ErrorBoundary
  Step 66  收藏功能乐观更新
  Step 67  搜索体验优化

第三阶段：测试体系                        Step 68–69
  Step 68  Vitest 与 React Testing Library
  Step 69  Playwright 端到端测试
  里程碑：v3.2

第四阶段：实时功能与后台管理               Step 70–71
  Step 70  SignalR 实时通知与通知持久化
  Step 71  Admin 数据面板

第五阶段：认证扩展、交付优化与总结          Step 72–75
  Step 72  Google OAuth 登录
  Step 73  CI/CD 优化与 Nginx 原理
  Step 74  Bundle 优化与 Web Vitals
  Step 75  V3 总结与项目收尾
  里程碑：v3.3
```

共 19 步：Step 57–75。
